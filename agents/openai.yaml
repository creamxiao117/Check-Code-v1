interface:
  display_name: "Check Code v1"
  short_description: "检查常见代码、配置文件和项目测试"
  default_prompt: "Use $check-code-v1 to inspect this project and report code quality issues without modifying files."

policy:
  allow_implicit_invocation: true
